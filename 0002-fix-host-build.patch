--- a/gcc/cccp.c
+++ b/gcc/cccp.c
@@ -94,8 +94,10 @@
 #define TOOL_INCLUDE_DIR ""
 
 // SN-Phil: windows kludge for directory sensing
+#ifdef _WIN32
 #define WIN32_LEAN_AND_MEAN
 #include <windows.h>
+#endif
 
 // SN-Phil: japanese support
 extern int host_is_japanese () ;
@@ -5208,10 +5210,12 @@
     // If fname is a dir name we wrongly generate an error of permission deinied. 
     // As we know this is meant to be a file we check for a dir and patch the
     // errno if so. (this has happened as STL includes dont have '.h'!)
+#ifdef _WIN32
     {
       long int attr = GetFileAttributes(fname) ;
       if ( attr == FILE_ATTRIBUTE_DIRECTORY ) // if its a dir
         errno = ENOENT ;    // fake theres not a file there
     } ;
+#endif
 
 	  return fd;
       }
--- a/gcc/SNMisc.c
+++ b/gcc/SNMisc.c
@@ -21,15 +21,19 @@
  * =============================================================== */
 
 
+#ifdef _WIN32
 // SN-Phil: windows kludge for directory sensing
 #define WIN32_LEAN_AND_MEAN
 #include <windows.h>
+#endif
 
 int host_is_japanese(void)
 {
+#ifdef _WIN32
   LANGID id = GetSystemDefaultLangID();
   if ( LANG_JAPANESE == PRIMARYLANGID(id) )
   {
    return 1;
   } ;
+#endif
   return 0;
 } ;
